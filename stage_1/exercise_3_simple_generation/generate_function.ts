function greetUser(name: string): string {
    return `Hello, ${name}! Welcome to Cody Training Workshop.`;
}

// Example user name
const userName: string = "Moshin";

// Call greetUser to display a greeting message
console.log(greetUser(userName));

// TODO: Use Cody to generate additional functions.
// Example: Consider creating a function to calculate a user's age from their birth year,
// Ask user where they live, how many children they have, their favourite programming language 
// Ask user their favourite game, favourite movie or book.

